/* Auto-generated-shape type declarations for pcr-dev.
 * Only the napi-exported `run(argv)` surface is public.
 */

/** Run the PCR CLI with the given argv. Returns the process exit code. */
export declare function run(argv: Array<string>): number;
